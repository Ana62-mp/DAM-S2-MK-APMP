# Opcional: reemplaza la IP por la del equipo que ejecuta el backend.
# En Expo Go local la app detecta automáticamente el equipo de Metro.
# Para un túnel de Expo o un backend remoto, define una URL accesible.
# EXPO_PUBLIC_API_URL=http://192.168.1.17:3001
